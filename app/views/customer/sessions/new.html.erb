<div class="container mt-4">
  <h4>ログイン</h4>

  <form class="mt-4 ml-3">
    <p>会員の方はこちらからログイン</p>

    <%= form_with model: @customer, url: customer_session_path, local: true do |f| %>
      <div class="form-group row mx-4">
        <%= f.label :メールアドレス, class:"col-md-2" %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "sample@example.com", class:"col-md-5" %>
      </div>

      <div class="form-group row mx-4">
        <%= f.label :パスワード, class:"col-md-2" %>
        <%= f.password_field :password, autocomplete: "current-password", class:"col-md-5" %>
      </div>

      <div class="form-group row mx-4">
        <%= f.submit "ログイン", class:"btn btn-primary offset-2 col-md-2" %>
      </div>
    <% end %>
  </form>

  <p class="mt-4 ml-3">会員登録がお済みでない方</p>
  <p class="mt-3 offset-1"><%= render "customer/shared/links" %>から新規登録を行ってください</p>


</div>